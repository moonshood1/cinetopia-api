const { editPassword } = require("./editPassword");
const { editProfile } = require("./editProfile");
const { getInformations } = require("./getInformations");
const { rateArticle } = require("./rateArticle");
const { saveArticle } = require("./saveArticle");

module.exports = {
  editPassword,
  editProfile,
  rateArticle,
  saveArticle,
  getInformations,
};
